server.port: 12307
spring:
  application:
    name: springclient
  profile:
    active: dev
#  boot:
#    admin:
#      url:  http://localhost:12300
  cloud:
    config:
      profile: dev
      name: springclient_dev #指定配置文件名字（多个配置文件以英文逗号隔开）
      label: develop #git仓库分支名
      discovery:
        enabled: true
        serviceId: springconfig #连接的配置中心名字（applicaiton.name）
  rabbitmq:
    host: 150.109.49.131
    port: 5672
    username: guest
    password: rabbitMaduar310
#  zipkin:
#    rabbitmq:
#      queue: zipkin
#    sleuth:
#      enable: false
#      sampler:
#        percentage: 1.0

params: xxxclient
management:
  security:
    enabled: false

#discovery_url: 150.109.49.131:8761
discovery_url: 127.0.0.1:8761
eureka:
  instance:
    hostname: ${spring.cloud.client.ipAddress}
    instanceId: ${spring.cloud.client.ipAddress}:${server.port}
    leaseRenewalIntervalInSeconds: 60  #心跳间隔时间1s (for Dev)
    leaseExpirationDurationInSeconds: 90  #连续2s未响应时将服务注销 (for Dev)
#    metadataMap:
#      zone: ali
  client:
#    preferSameZoneEureka: true
    serviceUrl:
      defaultZone: http://discoveryActuator:discoveryActuatorPwd@${discovery_url}/eureka/
